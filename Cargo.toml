[package]
name = "dotss"
version = "0.1.0-beta.38"
edition = "2024"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
