# Merge Checklist for Branch `feature/section-injections`

> [!IMPORTANT]
> REMINDER BEFORE MERGING INTO `develop` OR `main`:
> 1. Delete the temporary test installer script: `rm -f install-section-injections.sh`
> 2. Ensure `install.sh` remains clean and unchanged.
> 3. Verify all tests pass cleanly with `cargo test`.
